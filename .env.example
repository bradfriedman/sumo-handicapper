# Database Configuration
# Copy this file to .env and fill in your actual credentials

# ==============================================================================
# LOCAL DEVELOPMENT (using Cloud SQL Proxy)
# ==============================================================================
# Use these settings when running locally with Cloud SQL Proxy
# Start proxy: ./cloud-sql-proxy PROJECT:REGION:INSTANCE --port=3307

DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=dewsweeper
DB_NAME=dewsweeper3
DB_PASSWORD=your_password_here

# ==============================================================================
# PRODUCTION (using Cloud SQL Python Connector)
# ==============================================================================
# For Streamlit Cloud or production deployments, use these settings instead.
# When CLOUD_SQL_CONNECTION_NAME is set, the app will use Cloud SQL Python
# Connector instead of the proxy method.

# CLOUD_SQL_CONNECTION_NAME=PROJECT_ID:REGION:INSTANCE_NAME
# DB_USER=dewsweeper
# DB_NAME=dewsweeper3
# DB_PASSWORD=your_password_here
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# ==============================================================================
# Notes:
# - You only need ONE set of credentials (either proxy OR connector)
# - For local development, use the proxy method (DB_HOST/DB_PORT)
# - For Streamlit Cloud, use the connector method (CLOUD_SQL_CONNECTION_NAME)
# - Never commit this file with real credentials!
# ==============================================================================
